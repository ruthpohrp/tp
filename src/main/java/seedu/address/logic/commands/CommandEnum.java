package seedu.address.logic.commands;

/**
 * Represents an enumeration class of all of DukePro(f)'s possible commands
 */
public enum CommandEnum {
    ADD,
    CLEAR,
    COMMAND_SUMMARY,
    DELETE,
    EDIT,
    EXIT,
    FIND,
    HELP,
    LIST,
    NEXT_EVENT,
    UPCOMING_EVENTS;
}
